[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python -c 'print(\"ALIVE\"); import app; print(\"APP IMPORTED\")' 2>&1 || echo 'IMPORT FAILED'"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 1

# Supabase (model storage + feedback logging)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE=your-service-role-key

# Anthropic (Claude Vision fallback + auto-labeling)
ANTHROPIC_API_KEY=sk-ant-...

# Service config
PORT=8080
PARSER_TRAFFIC_PCT=70          # 70% ML model, 30% Claude Vision fallback
CONFIDENCE_THRESHOLD=0.70      # Below this → flag for review
MIN_PAGES_FOR_RETRAIN=50       # Auto-labeled pages before retrain is ready
LOG_LEVEL=INFO

# Cost tracking
CLAUDE_COST_PER_PAGE=0.003
